#include "Uuid.h"

#include "Random.h"

namespace Nebula // ---------------------------------------------------------------------------------------------------------------
{

DEBUG_ONLY(std::set<uint64_t> Uuid::s_uuids = {});

// --------------------------------------------------------------------------------------------------------------------------------

Uuid::Uuid() :
	m_value(Random::Integer<uint64_t>())
{
	ASSERT(s_uuids.insert(m_value).second);
}

// --------------------------------------------------------------------------------------------------------------------------------
Uuid::Uuid(Uuid const& other) :
	m_value(other.m_value)
{
}

} // namespace Nebula -------------------------------------------------------------------------------------------------------------
